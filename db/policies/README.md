# RLS policies

Row-level security policy SQL for each table, added starting with P1-04 (RLS helper functions +
canonical policy pattern). One file per table, applied as part of that table's migration.
